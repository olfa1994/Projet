{% extends 'base.html.twig' %}

{% block title %}Mon agence{% endblock %}

{% block body %}

<div class="container">
    <div id="carouselExampleSlidesOnly" class="carousel slide" data-ride="carousel">
        <div class="carousel-inner">
            <div class="carousel-item active">
                <img class="d-block w-100" src="/uploads/property/agence.jpg" alt="First slide" height="500px">
            </div>
            <div class="carousel-item">
                <img class="d-block w-100" src="/uploads/property/villa1.jpg" alt="Second slide" height="500px">
            </div>
            <div class="carousel-item">
                <img class="d-block w-100" src="/uploads/property/villa2.jpg" alt="Third slide" height="500px">
            </div>
        </div>
    </div>
    <div class="jumbotron text-center" >
        <h1>Bienvenue à mon agence</h1>
        <p>Mon agence est une agence immobilière. Là vous trouvez tous vos besoins de locations ou de ventes. <br> Si vous avez un bien à vendre ou à louer, veuillez nous contacter.</p>
    </div>
<div class="container" >
    <h2>Les biens phares à vendre</h2>
    <div class="row flex">
        {% for property in ventes%}
            <div class="col-lg-4 mb-4">
                <div class="card h-100 text-center">
                    <img class="card-img-top" src="{{ asset(property.path) }}" alt="{{ property.title }}">
                    <div class="card-body">
                        <a href="{{ path('property.show', {id : property.id}) }}" class="card-title">{{ property.title }}</a>
                        <h6 class="card-subtitle mb-2 text-muted">{{ property.price | number_format(0,'',' ')}} dt</h6>
                        <p class="card-text">{{ property.description }}</p>
                    </div>
                    <div class="card-footer">
                        <a href="{{ path('property.show', {id : property.id}) }}" class="btn btn-primary">Détails</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
</div>
    <h2>Les biens phares à louer</h2>
    <div class="row flex">
        {% for property in locations%}
            <div class="col-lg-4 mb-4">
                <div class="card h-100 text-center">
                    <img class="card-img-top" src="{{ asset(property.path) }}" alt="{{ property.title }}">
                    <div class="card-body">
                        <a href="{{ path('property.show', {id : property.id}) }}" class="card-title">{{ property.title }}</a>
                        <h6 class="card-subtitle mb-2 text-muted">{{ property.price | number_format(0,'',' ')}} dt</h6>
                        <p class="card-text">{{ property.description }}</p>
                    </div>
                    <div class="card-footer">
                        <a href="{{ path('property.show', {id : property.id}) }}" class="btn btn-primary">Détails</a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>


{% endblock %}
